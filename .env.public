SUI_FULLNODE_URL=https://fullnode.mainnet.sui.io:443

# ============================================
# Common Coin Types (Reference)
# ============================================
SUI_COIN_TYPE=0x2::sui::SUI
USDC_COIN_TYPE=0xdba34672e30cb065b1f93e3ab55318768fd6fef66c15942c9f7cb846e2f900e7::usdc::USDC
LBTC_COIN_TYPE=0x3e8e9423d80e1774a7ca128fccd8bf5f1f7753be658c5e645929037f7c819040::lbtc::LBTC
XBTC_COIN_TYPE=0x876a4b7bce8aeaef60464c11f4026903e9afacab79b9b142686158aa86560b50::xbtc::XBTC

# ============================================
# test:flash-loan (1_scallop_flash-loan-custom.ts)
# ============================================
# Scallop Flash Loan test - borrow and immediately repay
FLASH_LOAN_COIN_TYPE=0xdba34672e30cb065b1f93e3ab55318768fd6fef66c15942c9f7cb846e2f900e7::usdc::USDC
FLASH_LOAN_AMOUNT=1000000  # 1 USDC (6 decimals)

# ============================================
# test:suilend-deposit (2_suilend_deposit.ts)
# ============================================
# Deposit any asset to Suilend as collateral
DEPOSIT_COIN_TYPE=0x3e8e9423d80e1774a7ca128fccd8bf5f1f7753be658c5e645929037f7c819040::lbtc::LBTC
DEPOSIT_AMOUNT=1101
DEPOSIT_THRESHOLD=1101

# Example: SUI deposit
# DEPOSIT_COIN_TYPE=0x2::sui::SUI
# DEPOSIT_AMOUNT=500000000      # 0.5 SUI (9 decimals)
# DEPOSIT_THRESHOLD=400000000   # 0.4 SUI

# ============================================
# test:suilend-borrow (2_suilend_borrow.ts)
# ============================================
# Borrow from Suilend against your collateral
BORROW_COIN_TYPE=0xdba34672e30cb065b1f93e3ab55318768fd6fef66c15942c9f7cb846e2f900e7::usdc::USDC
BORROW_AMOUNT=500000     # 0.5 USDC (6 decimals)
BORROW_THRESHOLD=500000

# ============================================
# test:swap / test:swap-exec (3_7k_swap_*.ts)
# ============================================
# Swap tokens via 7k aggregator
# Swapping 0.5 USDC → LBTC (matching borrowed amount)
SWAP_INPUT_COIN_TYPE=0xdba34672e30cb065b1f93e3ab55318768fd6fef66c15942c9f7cb846e2f900e7::usdc::USDC
SWAP_OUTPUT_COIN_TYPE=0x3e8e9423d80e1774a7ca128fccd8bf5f1f7753be658c5e645929037f7c819040::lbtc::LBTC
SWAP_AMOUNT=500000  # 0.5 USDC (6 decimals)

# ============================================
# test:leverage / test:leverage-exec 
# (4_leverage_strategy_*.ts)
# ============================================
# SUI Leverage Strategy: Long SUI with USDC flash loan
# - Uses SUI as collateral
# - Borrows USDC via flash loan, swaps to SUI, deposits, borrows USDC to repay
# 
# DEPOSIT_AMOUNT: Initial SUI equity (in raw units, 9 decimals)
# MULTIPLIER: Target leverage (e.g., 1.5 = 1.5x leverage)
#
# Example: 0.1 SUI with 1.5x leverage
# DEPOSIT_AMOUNT=100000000    # 0.1 SUI
LEVERAGE_MULTIPLIER=1.5              # 1.5x leverage
# Sui test
LEVERAGE_DEPOSIT_COIN_TYPE=0x2::sui::SUI
LEVERAGE_DEPOSIT_AMOUNT=500000000    # 0.5 SUI (9 decimals)
# LBTC test
# LEVERAGE_DEPOSIT_COIN_TYPE=0x3e8e9423d80e1774a7ca128fccd8bf5f1f7753be658c5e645929037f7c819040::lbtc::LBTC
# LEVERAGE_DEPOSIT_AMOUNT=1101 # 0.00001101 LBTC (8 decimals)

# ============================================
# test:lbtc-leverage (5_lbtc_leverage_strategy.ts)
# ============================================
# LBTC Leverage Strategy: Long LBTC with USDC flash loan
# - Uses LBTC as collateral
# - LBTC LTV = 60%, Max Multiplier = 2.5x
#
# Uses DEPOSIT_COIN_TYPE, DEPOSIT_AMOUNT from above
# MULTIPLIER: Target leverage (max 2.5x for 60% LTV)
MULTIPLIER=1.5
